<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yunya.modules.system.mapper.PostMapper" >
  <resultMap id="BaseResultMap" type="com.yunya.models.system.Post" >
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="post_group_id" property="postGroupId" jdbcType="INTEGER" />
    <result column="order_num" property="orderNum" jdbcType="INTEGER" />
    <result column="allow_operation" property="allowOperation" jdbcType="BIT" />
    <result column="inservice" property="inservice" jdbcType="BIT" />
    <result column="crt_id" property="crtId" jdbcType="INTEGER" />
    <result column="crt_name" property="crtName" jdbcType="VARCHAR" />
    <result column="crt_time" property="crtTime" jdbcType="TIMESTAMP" />
    <result column="upd_id" property="updId" jdbcType="INTEGER" />
    <result column="upd_name" property="updName" jdbcType="VARCHAR" />
    <result column="upd_time" property="updTime" jdbcType="TIMESTAMP" />
  </resultMap>
  <!--根据条件查询岗位列表-->
  <select id="selectBrandByExample" resultType="com.yunya.modules.system.vo.PostVO">
    select
    p.id as id,
    p.name as `name`,
    p.post_group_id as postGroupId,
    p.allow_operation as allowOperation,
    pg.name as postGroupName,
    p.inservice as inservice
    from post p
    left join post_group pg on p.post_group_id = pg.id
    <where>
      <if test="form.id != null">
        and p.id = #{form.id}
      </if>
      <if test="form.name != null and form.name != ''">
        and p.name LIKE CONCAT(CONCAT('%',#{form.name}),'%')
      </if>
      <if test="form.groupIds != null and form.groupIds.size > 0">
        and p.post_group_id in
        <foreach collection="form.groupIds" item="item" index="index" open="(" close=")" separator=",">
          #{item}
        </foreach>
      </if>
      <if test="form.inservice != null">
        and p.inservice = #{form.inservice}
      </if>
    </where>
  </select>
</mapper>