<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yunya.modules.system.mapper.SysDistrictsMapper" >
  <resultMap id="BaseResultMap" type="com.yunya.models.system.SysDistricts" >
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="parent_id" property="parentId" jdbcType="INTEGER" />
    <result column="deep" property="deep" jdbcType="TINYINT" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="pinyin" property="pinyin" jdbcType="VARCHAR" />
    <result column="pinyin_short" property="pinyinShort" jdbcType="VARCHAR" />
    <result column="ext_name" property="extName" jdbcType="VARCHAR" />
    <result column="inservice" property="inservice" jdbcType="BIT" />
    <result column="crt_id" property="crtId" jdbcType="VARCHAR" />
    <result column="crt_time" property="crtTime" jdbcType="TIMESTAMP" />
    <result column="upd_id" property="updId" jdbcType="VARCHAR" />
    <result column="upd_time" property="updTime" jdbcType="TIMESTAMP" />
  </resultMap>
  <!-- 根据条件查询地区列表 -->
  <select id="selectDistrictsList" resultType="com.yunya.modules.system.vo.SysDistrictsVO">
    select
      sd.id as id,
      sd.parent_id as parentId,
      sd.deep as deep,
      sd.`name` as name,
      sd.ext_name as extName,
      sd.pinyin as pinyin,
      sd.pinyin_short as pinyinShort
    from sys_districts sd
    <where>
      <if test="queryForm.id != null ">
       and sd.id = #{queryForm.id}
      </if>
      <if test="queryForm.parentId != null ">
       and sd.parent_id = #{queryForm.parentId}
      </if>
      <if test="queryForm.deep != null ">
       and sd.deep = #{queryForm.deep}
      </if>
      <if test="queryForm.keyWord != null  and queryForm.keyWord != '' ">
       and sd.name like concat('%',#{queryForm.keyWord},'%')
       or sd.ext_name like concat('%',#{queryForm.keyWord},'%')
       or sd.pinyin like concat('%',#{queryForm.keyWord},'%')
       or sd.pinyin_short like concat('%',#{queryForm.keyWord},'%')
      </if>
    </where>
  </select>
  <!-- 根据ID删除地区 -->
  <delete id="deleteDistrictsByIds">
    delete from sys_districts where id in
    <foreach item="id" collection="array" open="(" separator="," close=")">
      #{id}
    </foreach>
  </delete>
</mapper>