<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yunya.modules.system.mapper.AccountTypeMapper">
    <resultMap id="BaseResultMap" type="com.yunya.models.system.AccountType">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="sys_default" property="sysDefault" jdbcType="BIT"/>
        <result column="inservice" property="inservice" jdbcType="BIT"/>
        <result column="crt_id" property="crtId" jdbcType="INTEGER"/>
        <result column="crt_name" property="crtName" jdbcType="VARCHAR"/>
        <result column="crt_time" property="crtTime" jdbcType="TIMESTAMP"/>
        <result column="upd_id" property="updId" jdbcType="INTEGER"/>
        <result column="upd_name" property="updName" jdbcType="VARCHAR"/>
        <result column="upd_time" property="updTime" jdbcType="TIMESTAMP"/>
    </resultMap>
    <!-- 根据ID查询入账方式分类信息 -->
    <select id="selectAccountTypeVOById" resultType="com.yunya.modules.system.vo.AccountTypeVO">
        select
            act.id,
            act.name,
            act.sys_default as sysDefault,
            act.inservice
        from account_type act
        where act.id = #{id}
    </select>
    <!-- 根据条件查询入账方式分类列表 -->
    <select id="selectAccountTypeVOList" resultType="com.yunya.modules.system.vo.AccountTypeVO">
        select
            act.id,
            act.name,
            act.sys_default as sysDefault,
            act.inservice
        from account_type act
        <where>
            <if test="queryForm.id != null">
                and act.id = #{queryForm.id}
            </if>
            <if test="queryForm.name != null and queryForm.name != ''">
                and act.name like concat('%',#{queryForm.name},'%')
            </if>
            <if test="queryForm.inservice != null">
                and act.inservice = #{queryForm.inservice}
            </if>
        </where>
    </select>
</mapper>