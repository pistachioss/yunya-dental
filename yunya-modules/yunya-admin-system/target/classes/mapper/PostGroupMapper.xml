<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yunya.modules.system.mapper.PostGroupMapper">
    <resultMap id="BaseResultMap" type="com.yunya.modules.system.entity.PostGroup">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="parent_id" property="parentId" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="order_num" property="orderNum" jdbcType="INTEGER"/>
        <result column="allow_operation" property="allowOperation" jdbcType="BIT"/>
        <result column="inservice" property="inservice" jdbcType="BIT"/>
        <result column="crt_id" property="crtId" jdbcType="INTEGER"/>
        <result column="crt_name" property="crtName" jdbcType="VARCHAR"/>
        <result column="crt_time" property="crtTime" jdbcType="TIMESTAMP"/>
        <result column="upd_id" property="updId" jdbcType="INTEGER"/>
        <result column="upd_time" property="updTime" jdbcType="TIMESTAMP"/>
        <result column="upd_name" property="updName" jdbcType="VARCHAR"/>
    </resultMap>
    <!-- 根据条件查询岗位组列表 -->
    <select id="selectPostGroupByExample" resultType="com.yunya.modules.system.vo.PostGroupVO">
        select
        pg.id,
        pg.parent_id as parentId,
        pg.name,
        pg.order_num as orderNum,
        pg.allow_operation as allowOperation,
        pg.inservice
        from post_group pg
        <where>
            <if test="queryForm.id != null">
                and pg.id = #{queryForm.id}
            </if>
            <if test="queryForm.name != null and queryForm.name != ''">
                and pg.name = #{queryForm.name}
            </if>
            <if test="queryForm.inservice != null">
                and pg.inservice = #{queryForm.inservice}
            </if>
        </where>
        order by parentId, orderNum
    </select>
    <!-- 递归查询当前ID及子节点ID -->
    <select id="selectChildIdList" resultType="java.lang.Integer">
    SELECT
        pg.id
    FROM
        (
        SELECT
                @ids AS _ids,
                ( SELECT @ids := GROUP_CONCAT( id ) FROM post_group WHERE FIND_IN_SET( parent_id, @ids ) ) AS cids
            FROM
                post_group,
                ( SELECT @ids := #{postGroupId}, @l := 0 ) b
            WHERE
                @ids IS NOT NULL
            ) id,
            post_group pg
        WHERE
            FIND_IN_SET( pg.id, _ids )
        ORDER BY
        id
  </select>
</mapper>