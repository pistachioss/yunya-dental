<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yunya.modules.employeeattend.mapper.AttendancePunchRecordMapper" >
  <resultMap id="BaseResultMap" type="com.yunya.models.employee_attend.AttendancePunchRecord" >
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="user_id" property="userId" jdbcType="INTEGER" />
    <result column="org_id" property="orgId" jdbcType="INTEGER" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="punch_date" property="punchDate" jdbcType="DATE" />
    <result column="punch_time" property="punchTime" jdbcType="TIME" />
    <result column="start_time" property="startTime" jdbcType="TIME" />
    <result column="end_time" property="endTime" jdbcType="TIME" />
    <result column="punch_type" property="punchType" jdbcType="TINYINT" />
    <result column="punch_status" property="punchStatus" jdbcType="TINYINT" />
    <result column="source" property="source" jdbcType="TINYINT" />
    <result column="attendance_address_id" property="attendanceAddressId" jdbcType="INTEGER" />
    <result column="longitude" property="longitude" jdbcType="VARCHAR" />
    <result column="latitude" property="latitude" jdbcType="VARCHAR" />
    <result column="punch_address" property="punchAddress" jdbcType="VARCHAR" />
    <result column="org_id" property="orgId" jdbcType="INTEGER" />
    <result column="source_id" property="sourceId" jdbcType="INTEGER" />
    <result column="es_id" property="esId" jdbcType="INTEGER" />
    <result column="is_punch" property="isPunch" jdbcType="TINYINT" />
    <result column="is_in_scope" property="isInScope" jdbcType="TINYINT" />
    <result column="wifi_mac_address" property="wifiMacAddress" jdbcType="VARCHAR" />
    <result column="crt_id" property="crtId" jdbcType="INTEGER" />
    <result column="crt_time" property="crtTime" jdbcType="TIMESTAMP" />
    <result column="upt_id" property="uptId" jdbcType="INTEGER" />
    <result column="upt_time" property="uptTime" jdbcType="TIMESTAMP" />
  </resultMap>

  <select id="findAttendancePunchRecordList" resultType="com.yunya.feign.employee_attend.vo.AttendancePunchRecordVO">
    select
        t.id,
        t.org_id as orgId,
        t.name,
        t.user_id as userId,
        t.punch_date as punchDate,
        t.punch_time as punchTime,
        t.punch_time as originalPunchTime,
        t.start_time as startTime,
        t.end_time as endTime,
        t.punch_type as punchType,
        t.punch_status as punchStatus,
        t.source,
        t.attendance_address_id as attendanceAddressId,
        t.longitude,
        t.latitude,
        t.punch_address as punchAddress,
        t.source_id as sourceId,
        t.es_id as esId,
        t.is_punch as isPunch,
        t.is_in_scope as isInScope,
        t.wifi_mac_address as wifiMacAddress
    from attendance_punch_record t
    <where>
      <if test="queryForm.userId != null" >
        and t.user_id = #{queryForm.userId}
      </if>
      <if test="queryForm.orgId != null" >
        and t.org_id = #{queryForm.orgId}
      </if>
      <if test="queryForm.punchDate != null" >
        and t.punch_date = DATE_FORMAT(#{queryForm.punchDate},'%Y-%m-%d')
      </if>
      <if test="queryForm.betweenDate!=null and queryForm.andDate!=null" >
        and t.punch_date between DATE_FORMAT(#{queryForm.betweenDate},'%Y-%m-%d') and DATE_FORMAT(#{queryForm.andDate},'%Y-%m-%d')
      </if>
      <if test="queryForm.source!=null">
        and t.source = #{queryForm.source}
      </if>
      <if test="queryForm.punchStatus!=null">
        and t.punch_status = #{queryForm.punchStatus}
      </if>
      <if test="queryForm.isPunch!=null">
        and t.is_punch = #{queryForm.isPunch}
      </if>
      <if test="queryForm.notEqualPunchStatus != null">
        and t.punch_status &lt;&gt; #{queryForm.notEqualPunchStatus}
      </if>
      <if test="queryForm.punchStatusList!=null and queryForm.punchStatusList.length>0">
        and t.punch_status in
        <foreach collection="queryForm.punchStatusList" item="item" index="index" open="(" close=")" separator=",">
          #{item}
        </foreach>
      </if>
      <if test="queryForm.notInIds!=null and queryForm.notInIds.size()>0">
        and t.id not in
        <foreach collection="queryForm.notInIds" item="item" index="index" open="(" close=")" separator=",">
          #{item}
        </foreach>
      </if>
    </where>
  </select>

  <select id="statisticsWorkDateOvertimeByMinute" resultType="com.yunya.feign.employee_attend.vo.AttendanceWorkDateOvertimeMinuteVO">
    select
        t.id,
        t.org_id as orgId,
        t.user_id as userId,
        t.punch_date as date,
        TIMESTAMPDIFF(MINUTE, DATE_FORMAT(t.end_time,'%Y-%m-%d %H:%i'), DATE_FORMAT(t.punch_time,'%Y-%m-%d %H:%i')) as minutes,
        t.punch_time as punchTime,
        t.punch_time as originalPunchTime,
        t.end_time as endTime,
        t.is_in_scope as isInScope,
        t.punch_address as punchAddress
    from attendance_punch_record t
    <where>
      TIMESTAMPDIFF(MINUTE, t.end_time, t.punch_time)>=#{minute}
      <if test="queryForm.userId != null" >
        and t.user_id = #{queryForm.userId}
      </if>
      <if test="queryForm.orgId != null" >
        and t.org_id = #{queryForm.orgId}
      </if>
      <if test="queryForm.punchDate != null" >
        and t.punch_date = DATE_FORMAT(#{queryForm.punchDate},'%Y-%m-%d')
      </if>
      <if test="queryForm.betweenDate!=null and queryForm.andDate!=null" >
        and t.punch_date between DATE_FORMAT(#{queryForm.betweenDate},'%Y-%m-%d') and DATE_FORMAT(#{queryForm.andDate},'%Y-%m-%d')
      </if>
      <if test="queryForm.source!=null">
        and t.source = #{queryForm.source}
      </if>
      <if test="queryForm.punchStatus!=null">
        and t.punch_status = #{queryForm.punchStatus}
      </if>
      <if test="queryForm.isPunch!=null">
        and t.is_punch = #{queryForm.isPunch}
      </if>
      <if test="queryForm.notEqualPunchStatus != null">
        and t.punch_status &lt;&gt; #{queryForm.notEqualPunchStatus}
      </if>
      <if test="queryForm.punchStatusList!=null and queryForm.punchStatusList.length>0">
        and t.punch_status in
        <foreach collection="queryForm.punchStatusList" item="item" index="index" open="(" close=")" separator=",">
          #{item}
        </foreach>
      </if>
      <if test="queryForm.notInIds!=null and queryForm.notInIds.size()>0">
        and t.id not in
        <foreach collection="queryForm.notInIds" item="item" index="index" open="(" close=")" separator=",">
          #{item}
        </foreach>
      </if>
    </where>
  </select>

  <select id="findAttendancePunchRecordWithScheduleIdList" resultType="com.yunya.feign.employee_attend.vo.AttendancePunchRecordVO">
    select
        t.id,
        t.org_id as orgId,
        t.name,
        t.user_id as userId,
        t.punch_date as punchDate,
        t.punch_time as punchTime,
        t.punch_time as originalPunchTime,
        t.start_time as startTime,
        t.end_time as endTime,
        t.punch_type as punchType,
        t.punch_status as punchStatus,
        t.source,
        t.es_id as esId,
        t.attendance_address_id as attendanceAddressId,
        t.longitude,
        t.latitude,
        t.punch_address as punchAddress,
        t.source_id as sourceId,
        t.is_punch as isPunch,
        t.is_in_scope as isInScope,
        t.wifi_mac_address as wifiMacAddress
      from attendance_punch_record t
      <where>
        <if test="queryForm.userId != null" >
            and t.user_id = #{queryForm.userId}
        </if>
        <if test="queryForm.orgId != null" >
            and t.org_id = #{queryForm.orgId}
        </if>
        <if test="queryForm.punchDate != null" >
            and DATE_FORMAT(t.punch_date,'%Y-%m-%d') = DATE_FORMAT(#{queryForm.punchDate},'%Y-%m-%d')
        </if>
        <if test="queryForm.betweenDate!=null and queryForm.andDate!=null " >
            and DATE_FORMAT(t.punch_date,'%Y-%m-%d') between DATE_FORMAT(#{queryForm.betweenDate},'%Y-%m-%d') and DATE_FORMAT(#{queryForm.andDate},'%Y-%m-%d')
        </if>
        <if test="queryForm.notEqualPunchStatus != null">
            and t.punch_status &lt;&gt; #{queryForm.notEqualPunchStatus}
        </if>
        <if test="queryForm.punchStatusList!=null and queryForm.punchStatusList.length>0">
            and t.punch_status in
            <foreach collection="queryForm.punchStatusList" item="item" index="index" open="(" close=")" separator=",">
                #{item}
            </foreach>
        </if>
     </where>
  </select>

  <select id="findAttendancePunchRecordListGroupByDate" resultType="com.yunya.feign.employee_attend.vo.AttendancePunchRecordVO">
        select
        t.id,
        t.org_id as orgId,
        t.name,
        t.user_id as userId,
        t.punch_date as punchDate,
        t.punch_time as punchTime,
        t.punch_time as originalPunchTime,
        t.start_time as startTime,
        t.end_time as endTime,
        t.punch_type as punchType,
        t.punch_status as punchStatus,
        t.source,
        t.attendance_address_id as attendanceAddressId,
        t.longitude,
        t.latitude,
        t.punch_address as punchAddress,
        t.source_id as sourceId,
        t.es_id as esId,
        t.is_punch as isPunch,
        t.is_in_scope as isInScope,
        t.wifi_mac_address as wifiMacAddress
        from attendance_punch_record t
        <where>
            <if test="queryForm.userId != null" >
                and t.user_id = #{queryForm.userId}
            </if>
            <if test="queryForm.orgId != null" >
                and t.org_id = #{queryForm.orgId}
            </if>
            <if test="queryForm.punchDate != null" >
                and t.punch_date = DATE_FORMAT(#{queryForm.punchDate},'%Y-%m-%d')
            </if>
            <if test="queryForm.betweenDate!=null and queryForm.andDate!=null" >
                and t.punch_date between DATE_FORMAT(#{queryForm.betweenDate},'%Y-%m-%d') and DATE_FORMAT(#{queryForm.andDate},'%Y-%m-%d')
            </if>
            <if test="queryForm.source!=null">
                and t.source = #{queryForm.source}
            </if>
            <if test="queryForm.sources!=null">
                and t.source not in
                <foreach collection="queryForm.sources" item="item" index="index" open="(" close=")" separator=",">
                    #{item}
                </foreach>
            </if>
            <if test="queryForm.punchStatus!=null">
                and t.punch_status = #{queryForm.punchStatus}
            </if>
            <if test="queryForm.isPunch!=null">
                and t.is_punch = #{queryForm.isPunch}
            </if>
            <if test="queryForm.notEqualPunchStatus != null">
                and t.punch_status &lt;&gt; #{queryForm.notEqualPunchStatus}
            </if>
            <if test="queryForm.punchStatusList!=null and queryForm.punchStatusList.length>0">
                and t.punch_status in
                <foreach collection="queryForm.punchStatusList" item="item" index="index" open="(" close=")" separator=",">
                    #{item}
                </foreach>
            </if>
            <if test="queryForm.notInIds!=null and queryForm.notInIds.size()>0">
                and t.id not in
                <foreach collection="queryForm.notInIds" item="item" index="index" open="(" close=")" separator=",">
                    #{item}
                </foreach>
            </if>
        </where>
        group by t.punch_date
        order by t.punch_date asc
    </select>
</mapper>