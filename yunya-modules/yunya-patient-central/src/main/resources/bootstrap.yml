# 默认启用开发环境配置
spring:
  application:
    name: yunya_patient_central
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true