<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yunya.report.ultimate.mapper.BaseBillPayMapper">
    <resultMap id="BaseResultMap" type="com.yunya.models.report.BaseBillPay">
        <!--
          WARNING - @mbg.generated
        -->
        <result column="bill_pay_id" property="billPayId" jdbcType="INTEGER"/>
        <result column="bill_id" property="billId" jdbcType="INTEGER"/>
        <result column="org_id" property="orgId" jdbcType="INTEGER"/>
        <result column="treatment_id" property="treatmentId" jdbcType="INTEGER"/>
        <result column="payee_user_id" property="payeeUserId" jdbcType="INTEGER"/>
        <result column="payee_date" property="payeeDate" jdbcType="TIMESTAMP"/>
        <result column="received_amount" property="receivedAmount" jdbcType="DECIMAL"/>
        <result column="still_owe_amount" property="stillOweAmount" jdbcType="DECIMAL"/>
    </resultMap>

    <!-- 根据账单ID查询收费记录列表 -->
    <select id="selectBillPayList" resultType="com.yunya.feign.report.domain.vo.BaseBillPayVO">
        select
            bbp.bill_pay_id as billPayId,
            date_format(bbp.payee_date, '%Y-%m-%d') as payeeDate,
            bbp.received_amount as receivedAmount,
            bbp.payee_user_id as payeeId,
            be.employee_name as payeeName
        from
            base_bill_pay bbp
            left join base_employee be on bbp.payee_user_id = user_id
        where bbp.bill_id = #{billId}
        order by bbp.payee_date desc
    </select>

    <!-- 根据条件查询账单支付记录列表 -->
    <select id="selectBillRecordOfPayList" resultType="com.yunya.feign.report.domain.vo.BillOfPayRecordVO">
        SELECT
        brp.toolDate,
        brp.orderDate,
        brp.billDate,
        brp.billPayId,
        brp.billNum,
        brp.patientId,
        brp.patientName,
        brp.mobile,
        brp.gender,
        brp.regDentistId,
        brp.regDentistName,
        brp.orderAmount,
        brp.privilegeAmount,
        brp.actualAmount,
        brp.receivedAmount,
        brp.stillOweAmount,
        brp.remainingOweAmount,
        brp.payeeUserId,
        brp.payeeUserName
        FROM
        (
        SELECT
        DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) AS toolDate,
        DATE_FORMAT( bb.order_date, '%Y-%m-%d' ) AS orderDate,
        DATE_FORMAT( bb.bill_date, '%Y-%m-%d' ) AS billDate,
        bbp.bill_pay_id AS billPayId,
        bb.bill_num AS billNum,
        tp.patient_id AS patientId,
        tp.`name` AS patientName,
        tp.pinyin_name AS pinyinName,
        tp.mobile AS mobile,
        tp.gender AS gender,
        tp.registered_dentist_id AS regDentistId,
        tp.regDentistName AS regDentistName,
        bb.order_amount AS orderAmount,
        bb.privilege_amount AS privilegeAmount,
        bb.actual_amount AS actualAmount,
        bbp.org_id as orgId,
        bbp.received_amount AS receivedAmount,
        bbp.still_owe_amount AS stillOweAmount,
        bb.debt_amount AS remainingOweAmount,
        bbp.payee_user_id AS payeeUserId,
        be.employee_name AS payeeUserName
        FROM
        base_bill_pay bbp
        LEFT JOIN base_bill bb ON bbp.bill_id = bb.bill_id
        LEFT JOIN base_employee be ON bbp.payee_user_id = be.user_id
        LEFT JOIN (
        SELECT
        btp.treatment_id,
        bp.patient_id,
        bp.`name`,
        bp.pinyin_name,
        bp.mobile,
        bp.gender,
        btp.registered_dentist_id,
        be.employee_name AS regDentistName
        FROM
        base_treatment_process btp
        LEFT JOIN base_patient bp ON btp.patient_id = bp.patient_id
        LEFT JOIN base_employee be ON btp.registered_dentist_id = be.user_id
        ) tp ON bbp.treatment_id = tp.treatment_id
        ) brp
        <where>
            <if test="query.orgId != null">
                and brp.orgId = #{query.orgId}
            </if>
            <if test="query.billNum != null and query.billNum != ''">
                and brp.billNum like concat('%', #{query.billNum}, '%')
            </if>
            <if test="query.keyWord != null and query.keyWord != ''">
                and concat(IFNULL(brp.patientName, ''),IFNULL(brp.pinyinName, ''), IFNULL(brp.mobile, '')) like
                concat('%', #{query.keyWord}, '%')
            </if>
            <if test="query.tollStartDate != null and query.tollStartDate != ''">
                and brp.toolDate &gt;= #{query.tollStartDate}
            </if>
            <if test="query.tollEndDate != null and query.tollEndDate != ''">
                and brp.toolDate &lt;= #{query.tollEndDate}
            </if>
            <if test="query.billStartDate != null and query.billStartDate != ''">
                and brp.billDate &gt;= #{query.billStartDate}
            </if>
            <if test="query.billEndDate != null and query.billEndDate != ''">
                and brp.billDate &lt;= #{query.billEndDate}
            </if>
            <if test="query.regDentistIds != null and query.regDentistIds.length > 0">
                and brp.regDentistId in
                <foreach collection="query.regDentistIds" item="item" index="index" open="(" close=")" separator=",">
                    #{item}
                </foreach>
            </if>
        </where>
        ORDER BY
        brp.billPayId DESC
    </select>

    <!-- 根据条件查询门诊收费数据总览 -->
    <select id="selectClinicTollDataStatistic"
            resultType="com.yunya.feign.report.domain.vo.TollDataStatisticsVO">
        SELECT
        IFNULL(bbp.totalReceivedDebtAmount, 0) AS totalReceivedDebtAmount,
        IFNULL(bbp1.totalReceivedAmount, 0) AS totalReceivedAmount,
        IFNULL(br.totalBillRefundAmount, 0) AS totalBillRefundAmount,
        IFNULL(bbp2.totalClinicCollectionAmount, 0) AS totalClinicCollectionAmount,
        IFNULL(bbp3.totalClinicCollectedAmount, 0) AS totalClinicCollectedAmount
        FROM
        (
        SELECT
        SUM( bbp.received_amount ) AS totalReceivedDebtAmount
        FROM
        (
        SELECT
        bbp.received_amount,
        bbp.payee_date,
        bb.bill_date
        FROM
        base_bill_pay bbp
        LEFT JOIN base_bill bb ON bbp.bill_id = bb.bill_id
        <where>
            <if test="query.orgIds != null and query.orgIds.length > 0">
                and bbp.org_id in
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d') &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m') &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y') &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bbp
        WHERE
        bbp.payee_date != bbp.bill_date
        ) bbp,
        (
        SELECT
        SUM( bbp.received_amount ) AS totalReceivedAmount
        FROM
        base_bill_pay bbp
        <where>
            <if test="query.orgIds != null and query.orgIds.length > 0">
                and bbp.org_id in
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d') &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m') &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y') &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bbp1,(
        SELECT
        SUM( br.refund_amount ) AS totalBillRefundAmount
        FROM
        base_refund br
        <where>
            <if test="query.orgIds != null and query.orgIds.length > 0">
                and br.org_id in
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( br.refund_date, '%Y-%m-%d') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( br.refund_date, '%Y-%m-%d') &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( br.refund_date, '%Y-%m') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( br.refund_date, '%Y-%m') &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( br.refund_date, '%Y') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( br.refund_date, '%Y') &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) br,
        (
        SELECT
        SUM( bbp.received_amount ) AS totalClinicCollectionAmount
        FROM
        (
        SELECT
        bbp.received_amount
        FROM
        base_bill_pay bbp
        <where>
            <if test="query.orgIds != null and query.orgIds.length > 0">
                and bbp.org_id in
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d') &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m') &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y') &lt;= #{query.endDate}
                </if>
            </if>
            AND bbp.bill_id NOT IN (
            SELECT
            bb.bill_id
            FROM base_bill bb
            <where>
                <if test="query.orgIds != null and query.orgIds.length > 0">
                    and bb.org_id in
                    <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                        #{item}
                    </foreach>
                </if>
            </where>
            )
        </where>
        ) bbp
        ) bbp2,
        (
        SELECT
        SUM( bbp.received_amount ) AS totalClinicCollectedAmount
        FROM
        (
        SELECT
        bbp.received_amount
        FROM
        base_bill_pay bbp
        <where>
            <if test="query.orgIds != null and query.orgIds.length > 0">
                and bbp.org_id NOT IN
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d') &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y-%m') &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y') &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    and DATE_FORMAT( bbp.payee_date, '%Y') &lt;= #{query.endDate}
                </if>
            </if>
            AND bbp.bill_id IN (
            SELECT
            bb.bill_id
            FROM
            base_bill bb
            <where>
                <if test="query.orgIds != null and query.orgIds.length > 0">
                    and bb.org_id IN
                    <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                        #{item}
                    </foreach>
                </if>
            </where>
            )
        </where>
        ) bbp
        ) bbp3
    </select>

    <!-- 根据条件查询门诊当前月账单当前月收费总额 -->
    <select id="selectCurrentMonthTotalReceivedAmount" resultType="java.math.BigDecimal">
        SELECT
        SUM( bbp.received_amount )
        FROM
        base_bill_pay bbp
        INNER JOIN base_bill bb ON bbp.bill_id = bb.bill_id
        <where>
            <if test="query.queryDate != null and query.queryDate != ''">
                AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) = #{query.queryDate}
                AND DATE_FORMAT( bb.bill_date, '%Y-%m' ) = #{query.queryDate}
            </if>
            <if test="query.orgId != null">
                and bb.org_id = #{query.orgId}
            </if>
        </where>
    </select>

    <!-- 根据条件查询账单（当月账单当月收费）收费详情信息列表 -->
    <select id="selectBillChargeDetailInfoList"
            resultType="com.yunya.feign.report.domain.vo.StatementBillChargeDetailVO">
        SELECT
        billPayId,
        payeeDate,
        payeeOrgId,
        payeeOrgName,
        billId,
        billNum,
        billOrgId,
        billOrgName,
        billDate,
        patientId,
        patientName,
        patientMobile,
        regDentistId,
        regDentistName,
        originalAmount,
        privilegeAmount,
        actualAmount,
        totalReceivedAmount,
        receivedAmount,
        payeeId,
        payeeName
        FROM (
        SELECT
        bbp.billPayId,
        bbp.payeeDate,
        bbp.payeeOrgId,
        bbp.payeeOrgName,
        bbp.payeeId,
        bbp.payeeName,
        bbp.receivedAmount,
        bbp.billId,
        bbp.billOrgId,
        bo.abbreviation AS billOrgName,
        bbp.billNum,
        bbp.originalAmount,
        bbp.billDate,
        bbp.privilegeAmount,
        bbp.actualAmount,
        bbp.totalReceivedAmount,
        bbp.regDentistId,
        be.employee_name AS regDentistName,
        bbp.patientId,
        bp.`name` AS patientName,
        bp.mobile AS patientMobile,
        bp.pinyin_name AS pinyinName
        FROM
        (
        SELECT
        bbp.bill_pay_id AS billPayId,
        bbp.bill_id AS billId,
        bbp.org_id AS payeeOrgId,
        bo.abbreviation AS payeeOrgName,
        DATE_FORMAT(bbp.payee_date, '%Y-%m-%d') AS payeeDate,
        bbp.payee_user_id AS payeeId,
        be.employee_name AS payeeName,
        bbp.received_amount AS receivedAmount,
        bb.org_id AS billOrgId,
        bb.bill_num AS billNum,
        bb.order_amount AS originalAmount,
        DATE_FORMAT(bb.bill_date, '%Y-%m-%d') AS billDate,
        bb.privilege_amount AS privilegeAmount,
        bb.actual_amount AS actualAmount,
        bb.received_amount AS totalReceivedAmount,
        btp.registered_dentist_id AS regDentistId,
        btp.patient_id AS patientId
        FROM
        base_bill_pay bbp
        INNER JOIN base_organization bo ON bbp.org_id = bo.org_id
        INNER JOIN base_bill bb ON bbp.org_id = bb.org_id
        AND bbp.bill_id = bb.bill_id
        AND bbp.payee_date = bb.bill_date
        LEFT JOIN base_treatment_process btp ON bbp.treatment_id = btp.treatment_id
        INNER JOIN base_employee be ON bbp.payee_user_id = be.user_id
        <where>
            <if test="query.orgId != null">
                AND bbp.org_id = #{query.orgId}
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bbp
        LEFT JOIN base_patient bp ON bbp.patientId = bp.patient_id
        LEFT JOIN base_organization bo on bbp.billOrgId = bo.org_id
        LEFT JOIN base_employee be on bbp.regDentistId = be.user_id
        ) bbp
        <where>
            <if test="query.patientKeyword != null and query.patientKeyword != ''">
                AND concat(IFNULL(bbp.patientName, ''),IFNULL(bbp.pinyinName, ''), IFNULL(bbp.patientMobile, '')) like
                concat('%', #{query.patientKeyword}, '%')
            </if>
            <if test="query.billNum != null and query.billNum != ''">
                AND bbp.billNum LIKE concat('%', #{query.billNum}, '%')
            </if>
            <if test="query.chargeStartDate != null and query.chargeStartDate != ''">
                AND bbp.payeeDate &gt;= #{query.chargeStartDate}
            </if>
            <if test="query.chargeEndDate != null and query.chargeEndDate != ''">
                AND bbp.payeeDate &lt;= #{query.chargeEndDate}
            </if>
        </where>
        ORDER BY bbp.payeeDate DESC, bbp.billNum DESC
    </select>

    <!-- 根据条件查询账单收欠费（本月账单本月非首次收费）详情信息列表 -->
    <select id="selectBillCurrentChargeDebtDetailList"
            resultType="com.yunya.feign.report.domain.vo.StatementBillChargeDetailVO">
        SELECT
        billPayId,
        payeeDate,
        payeeOrgId,
        payeeOrgName,
        billId,
        billNum,
        billOrgId,
        billOrgName,
        billDate,
        patientId,
        patientName,
        patientMobile,
        regDentistId,
        regDentistName,
        originalAmount,
        privilegeAmount,
        actualAmount,
        totalReceivedAmount,
        receivedAmount,
        payeeId,
        payeeName
        FROM (
        SELECT
        bbp.billPayId,
        bbp.payeeDate,
        bbp.payeeOrgId,
        bbp.payeeOrgName,
        bbp.payeeId,
        bbp.payeeName,
        bbp.receivedAmount,
        bbp.billId,
        bbp.billOrgId,
        bo.abbreviation AS billOrgName,
        bbp.billNum,
        bbp.originalAmount,
        bbp.billDate,
        bbp.privilegeAmount,
        bbp.actualAmount,
        bbp.totalReceivedAmount,
        bbp.regDentistId,
        be.employee_name AS regDentistName,
        bbp.patientId,
        bp.`name` AS patientName,
        bp.mobile AS patientMobile,
        bp.pinyin_name AS pinyinName
        FROM
        (
        SELECT
        bbp.bill_pay_id AS billPayId,
        bbp.bill_id AS billId,
        bbp.org_id AS payeeOrgId,
        bo.abbreviation AS payeeOrgName,
        DATE_FORMAT(bbp.payee_date, '%Y-%m-%d') AS payeeDate,
        bbp.payee_user_id AS payeeId,
        be.employee_name AS payeeName,
        bbp.received_amount AS receivedAmount,
        bb.org_id AS billOrgId,
        bb.bill_num AS billNum,
        bb.order_amount AS originalAmount,
        DATE_FORMAT(bb.bill_date, '%Y-%m-%d') AS billDate,
        bb.privilege_amount AS privilegeAmount,
        bb.actual_amount AS actualAmount,
        bb.received_amount AS totalReceivedAmount,
        btp.registered_dentist_id AS regDentistId,
        btp.patient_id AS patientId
        FROM
        base_bill_pay bbp
        INNER JOIN base_organization bo ON bbp.org_id = bo.org_id
        INNER JOIN (
        SELECT
        bb.org_id,
        bb.bill_id,
        bb.bill_num,
        bb.order_amount,
        bb.bill_date,
        bb.privilege_amount,
        bb.actual_amount,
        bb.received_amount
        FROM
        base_bill bb
        <where>
            AND bb.bill_date IS NOT NULL
            <if test="query.orgId != null">
                AND bb.org_id = #{query.orgId}
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bb ON bbp.org_id = bb.org_id
        AND bbp.bill_id = bb.bill_id
        AND bbp.payee_date != bb.bill_date
        LEFT JOIN base_treatment_process btp ON bbp.treatment_id = btp.treatment_id
        INNER JOIN base_employee be ON bbp.payee_user_id = be.user_id
        <where>
            <if test="query.orgId != null">
                AND bbp.org_id = #{query.orgId}
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bbp
        LEFT JOIN base_patient bp ON bbp.patientId = bp.patient_id
        LEFT JOIN base_organization bo on bbp.billOrgId = bo.org_id
        LEFT JOIN base_employee be on bbp.regDentistId = be.user_id
        ) bbp
        <where>
            <if test="query.patientKeyword != null and query.patientKeyword != ''">
                AND concat(IFNULL(bbp.patientName, ''),IFNULL(bbp.pinyinName, ''), IFNULL(bbp.patientMobile, '')) like
                concat('%', #{query.patientKeyword}, '%')
            </if>
            <if test="query.billNum != null and query.billNum != ''">
                AND bbp.billNum LIKE concat('%', #{query.billNum}, '%')
            </if>
            <if test="query.chargeStartDate != null and query.chargeStartDate != ''">
                AND bbp.payeeDate &gt;= #{query.chargeStartDate}
            </if>
            <if test="query.chargeEndDate != null and query.chargeEndDate != ''">
                AND bbp.payeeDate &lt;= #{query.chargeEndDate}
            </if>
        </where>
        ORDER BY bbp.payeeDate DESC, bbp.billNum DESC
    </select>

    <!-- 根据条件查询账单收欠费（非本月账单本月非首次收费）详情信息列表 -->
    <select id="selectBillOtherChargeDebtDetailList"
            resultType="com.yunya.feign.report.domain.vo.StatementBillChargeDetailVO">
        SELECT
        billPayId,
        payeeDate,
        payeeOrgId,
        payeeOrgName,
        billId,
        billNum,
        billOrgId,
        billOrgName,
        billDate,
        patientId,
        patientName,
        patientMobile,
        regDentistId,
        regDentistName,
        originalAmount,
        privilegeAmount,
        actualAmount,
        totalReceivedAmount,
        receivedAmount,
        payeeId,
        payeeName
        FROM (
        SELECT
        bbp.billPayId,
        bbp.payeeDate,
        bbp.payeeOrgId,
        bbp.payeeOrgName,
        bbp.payeeId,
        bbp.payeeName,
        bbp.receivedAmount,
        bbp.billId,
        bbp.billOrgId,
        bo.abbreviation AS billOrgName,
        bbp.billNum,
        bbp.originalAmount,
        bbp.billDate,
        bbp.privilegeAmount,
        bbp.actualAmount,
        bbp.totalReceivedAmount,
        bbp.regDentistId,
        be.employee_name AS regDentistName,
        bbp.patientId,
        bp.`name` AS patientName,
        bp.mobile AS patientMobile,
        bp.pinyin_name AS pinyinName
        FROM
        (
        SELECT
        bbp.bill_pay_id AS billPayId,
        bbp.bill_id AS billId,
        bbp.org_id AS payeeOrgId,
        bo.abbreviation AS payeeOrgName,
        DATE_FORMAT(bbp.payee_date, '%Y-%m-%d') AS payeeDate,
        bbp.payee_user_id AS payeeId,
        be.employee_name AS payeeName,
        bbp.received_amount AS receivedAmount,
        bb.org_id AS billOrgId,
        bb.bill_num AS billNum,
        bb.order_amount AS originalAmount,
        DATE_FORMAT(bb.bill_date, '%Y-%m-%d') AS billDate,
        bb.privilege_amount AS privilegeAmount,
        bb.actual_amount AS actualAmount,
        bb.received_amount AS totalReceivedAmount,
        btp.registered_dentist_id AS regDentistId,
        btp.patient_id AS patientId
        FROM
        base_bill_pay bbp
        INNER JOIN base_organization bo ON bbp.org_id = bo.org_id
        INNER JOIN (
        SELECT
        bb.org_id,
        bb.bill_id,
        bb.bill_num,
        bb.order_amount,
        bb.bill_date,
        bb.privilege_amount,
        bb.actual_amount,
        bb.received_amount
        FROM
        base_bill bb
        <where>
            AND bb.bill_date IS NOT NULL
            <if test="query.orgId != null">
                AND bb.org_id = #{query.orgId}
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m-%d' ) &lt; #{query.startDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m' ) &lt; #{query.startDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y' ) &lt; #{query.startDate}
                </if>
            </if>
        </where>
        ) bb ON bbp.org_id = bb.org_id
        AND bbp.bill_id = bb.bill_id
        AND bbp.payee_date != bb.bill_date
        LEFT JOIN base_treatment_process btp ON bbp.treatment_id = btp.treatment_id
        INNER JOIN base_employee be ON bbp.payee_user_id = be.user_id
        <where>
            <if test="query.orgId != null">
                AND bbp.org_id = #{query.orgId}
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bbp
        LEFT JOIN base_patient bp ON bbp.patientId = bp.patient_id
        LEFT JOIN base_organization bo on bbp.billOrgId = bo.org_id
        LEFT JOIN base_employee be on bbp.regDentistId = be.user_id
        ) bbp
        <where>
            <if test="query.patientKeyword != null and query.patientKeyword != ''">
                AND concat(IFNULL(bbp.patientName, ''),IFNULL(bbp.pinyinName, ''), IFNULL(bbp.patientMobile, '')) like
                concat('%', #{query.patientKeyword}, '%')
            </if>
            <if test="query.billNum != null and query.billNum != ''">
                AND bbp.billNum LIKE concat('%', #{query.billNum}, '%')
            </if>
            <if test="query.chargeStartDate != null and query.chargeStartDate != ''">
                AND bbp.payeeDate &gt;= #{query.chargeStartDate}
            </if>
            <if test="query.chargeEndDate != null and query.chargeEndDate != ''">
                AND bbp.payeeDate &lt;= #{query.chargeEndDate}
            </if>
        </where>
        ORDER BY
        bbp.payeeDate DESC,
        bbp.billNum DESC
    </select>

    <!-- 根据条件查询门诊代收（当月）账单详情列表 -->
    <select id="selectCurrentBillCollectionDetailList"
            resultType="com.yunya.feign.report.domain.vo.StatementBillChargeDetailVO">
        SELECT
        billPayId,
        payeeDate,
        payeeOrgId,
        payeeOrgName,
        billId,
        billNum,
        billOrgId,
        billOrgName,
        billDate,
        patientId,
        patientName,
        patientMobile,
        regDentistId,
        regDentistName,
        originalAmount,
        privilegeAmount,
        actualAmount,
        totalReceivedAmount,
        receivedAmount,
        payeeId,
        payeeName
        FROM (
        SELECT
        bbp.billPayId,
        bbp.payeeDate,
        bbp.payeeOrgId,
        bbp.payeeOrgName,
        bbp.payeeId,
        bbp.payeeName,
        bbp.receivedAmount,
        bbp.billId,
        bbp.billOrgId,
        bo.abbreviation AS billOrgName,
        bbp.billNum,
        bbp.originalAmount,
        bbp.billDate,
        bbp.privilegeAmount,
        bbp.actualAmount,
        bbp.totalReceivedAmount,
        bbp.regDentistId,
        be.employee_name AS regDentistName,
        bbp.patientId,
        bp.`name` AS patientName,
        bp.mobile AS patientMobile,
        bp.pinyin_name AS pinyinName
        FROM
        (
        SELECT
        bbp.bill_pay_id AS billPayId,
        bbp.bill_id AS billId,
        bbp.org_id AS payeeOrgId,
        bo.abbreviation AS payeeOrgName,
        DATE_FORMAT(bbp.payee_date, '%Y-%m-%d') AS payeeDate,
        bbp.payee_user_id AS payeeId,
        be.employee_name AS payeeName,
        bbp.received_amount AS receivedAmount,
        bb.org_id AS billOrgId,
        bb.bill_num AS billNum,
        bb.order_amount AS originalAmount,
        DATE_FORMAT(bb.bill_date, '%Y-%m-%d') AS billDate,
        bb.privilege_amount AS privilegeAmount,
        bb.actual_amount AS actualAmount,
        bb.received_amount AS totalReceivedAmount,
        btp.registered_dentist_id AS regDentistId,
        btp.patient_id AS patientId
        FROM
        base_bill_pay bbp
        INNER JOIN base_organization bo ON bbp.org_id = bo.org_id
        INNER JOIN (
        SELECT
        bb.org_id,
        bb.bill_id,
        bb.bill_num,
        bb.order_amount,
        bb.bill_date,
        bb.privilege_amount,
        bb.actual_amount,
        bb.received_amount
        FROM
        base_bill bb
        <where>
            AND bb.bill_date IS NOT NULL
            <if test="query.orgId != null">
                AND bb.org_id != #{query.orgId}
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bb ON bbp.bill_id = bb.bill_id
        LEFT JOIN base_treatment_process btp ON bbp.treatment_id = btp.treatment_id
        INNER JOIN base_employee be ON bbp.payee_user_id = be.user_id
        <where>
            <if test="query.orgId != null">
                AND bbp.org_id = #{query.orgId}
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bbp
        LEFT JOIN base_patient bp ON bbp.patientId = bp.patient_id
        LEFT JOIN base_organization bo on bbp.billOrgId = bo.org_id
        LEFT JOIN base_employee be on bbp.regDentistId = be.user_id
        ) bbp
        <where>
            <if test="query.patientKeyword != null and query.patientKeyword != ''">
                AND concat(IFNULL(bbp.patientName, ''),IFNULL(bbp.pinyinName, ''), IFNULL(bbp.patientMobile, '')) like
                concat('%', #{query.patientKeyword}, '%')
            </if>
            <if test="query.billNum != null and query.billNum != ''">
                AND bbp.billNum LIKE concat('%', #{query.billNum}, '%')
            </if>
            <if test="query.chargeStartDate != null and query.chargeStartDate != ''">
                AND bbp.payeeDate &gt;= #{query.chargeStartDate}
            </if>
            <if test="query.chargeEndDate != null and query.chargeEndDate != ''">
                AND bbp.payeeDate &lt;= #{query.chargeEndDate}
            </if>
        </where>
        ORDER BY
        bbp.payeeDate DESC,
        bbp.billNum DESC
    </select>

    <!-- 根据条件查询门诊代收（非当月）账单详情列表 -->
    <select id="selectOtherBillCollectionDetailList"
            resultType="com.yunya.feign.report.domain.vo.StatementBillChargeDetailVO">
        SELECT
        billPayId,
        payeeDate,
        payeeOrgId,
        payeeOrgName,
        billId,
        billNum,
        billOrgId,
        billOrgName,
        billDate,
        patientId,
        patientName,
        patientMobile,
        regDentistId,
        regDentistName,
        originalAmount,
        privilegeAmount,
        actualAmount,
        totalReceivedAmount,
        receivedAmount,
        payeeId,
        payeeName
        FROM (
        SELECT
        bbp.billPayId,
        bbp.payeeDate,
        bbp.payeeOrgId,
        bbp.payeeOrgName,
        bbp.payeeId,
        bbp.payeeName,
        bbp.receivedAmount,
        bbp.billId,
        bbp.billOrgId,
        bo.abbreviation AS billOrgName,
        bbp.billNum,
        bbp.originalAmount,
        bbp.billDate,
        bbp.privilegeAmount,
        bbp.actualAmount,
        bbp.totalReceivedAmount,
        bbp.regDentistId,
        be.employee_name AS regDentistName,
        bbp.patientId,
        bp.`name` AS patientName,
        bp.mobile AS patientMobile,
        bp.pinyin_name AS pinyinName
        FROM
        (
        SELECT
        bbp.bill_pay_id AS billPayId,
        bbp.bill_id AS billId,
        bbp.org_id AS payeeOrgId,
        bo.abbreviation AS payeeOrgName,
        DATE_FORMAT(bbp.payee_date, '%Y-%m-%d') AS payeeDate,
        bbp.payee_user_id AS payeeId,
        be.employee_name AS payeeName,
        bbp.received_amount AS receivedAmount,
        bb.org_id AS billOrgId,
        bb.bill_num AS billNum,
        bb.order_amount AS originalAmount,
        DATE_FORMAT(bb.bill_date, '%Y-%m-%d') AS billDate,
        bb.privilege_amount AS privilegeAmount,
        bb.actual_amount AS actualAmount,
        bb.received_amount AS totalReceivedAmount,
        btp.registered_dentist_id AS regDentistId,
        btp.patient_id AS patientId
        FROM
        base_bill_pay bbp
        INNER JOIN base_organization bo ON bbp.org_id = bo.org_id
        INNER JOIN (
        SELECT
        bb.org_id,
        bb.bill_id,
        bb.bill_num,
        bb.order_amount,
        bb.bill_date,
        bb.privilege_amount,
        bb.actual_amount,
        bb.received_amount
        FROM
        base_bill bb
        <where>
            AND bb.bill_date IS NOT NULL
            <if test="query.orgId != null">
                AND bb.org_id != #{query.orgId}
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m-%d' ) &lt; #{query.startDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m' ) &lt; #{query.startDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y' ) &lt; #{query.startDate}
                </if>
            </if>
        </where>
        ) bb ON bbp.bill_id = bb.bill_id
        LEFT JOIN base_treatment_process btp ON bbp.treatment_id = btp.treatment_id
        INNER JOIN base_employee be ON bbp.payee_user_id = be.user_id
        <where>
            <if test="query.orgId != null">
                AND bbp.org_id = #{query.orgId}
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bbp
        LEFT JOIN base_patient bp ON bbp.patientId = bp.patient_id
        LEFT JOIN base_organization bo on bbp.billOrgId = bo.org_id
        LEFT JOIN base_employee be on bbp.regDentistId = be.user_id
        ) bbp
        <where>
            <if test="query.patientKeyword != null and query.patientKeyword != ''">
                AND concat(IFNULL(bbp.patientName, ''),IFNULL(bbp.pinyinName, ''), IFNULL(bbp.patientMobile, '')) like
                concat('%', #{query.patientKeyword}, '%')
            </if>
            <if test="query.billNum != null and query.billNum != ''">
                AND bbp.billNum LIKE concat('%', #{query.billNum}, '%')
            </if>
            <if test="query.chargeStartDate != null and query.chargeStartDate != ''">
                AND bbp.payeeDate &gt;= #{query.chargeStartDate}
            </if>
            <if test="query.chargeEndDate != null and query.chargeEndDate != ''">
                AND bbp.payeeDate &lt;= #{query.chargeEndDate}
            </if>
        </where>
        ORDER BY
        bbp.payeeDate DESC,
        bbp.billNum DESC
    </select>

    <!-- 根据条件查询门诊被代收账单（当月）明细列表 -->
    <select id="selectCurrentBillIsAcceptedDetailList"
            resultType="com.yunya.feign.report.domain.vo.StatementBillChargeDetailVO">
        SELECT
        billPayId,
        payeeDate,
        payeeOrgId,
        payeeOrgName,
        billId,
        billNum,
        billOrgId,
        billOrgName,
        billDate,
        patientId,
        patientName,
        patientMobile,
        regDentistId,
        regDentistName,
        originalAmount,
        privilegeAmount,
        actualAmount,
        totalReceivedAmount,
        receivedAmount,
        payeeId,
        payeeName
        FROM (
        SELECT
        bbp.billPayId,
        bbp.payeeDate,
        bbp.payeeOrgId,
        bbp.payeeOrgName,
        bbp.payeeId,
        bbp.payeeName,
        bbp.receivedAmount,
        bbp.billId,
        bbp.billOrgId,
        bo.abbreviation AS billOrgName,
        bbp.billNum,
        bbp.originalAmount,
        bbp.billDate,
        bbp.privilegeAmount,
        bbp.actualAmount,
        bbp.totalReceivedAmount,
        bbp.regDentistId,
        be.employee_name AS regDentistName,
        bbp.patientId,
        bp.`name` AS patientName,
        bp.mobile AS patientMobile,
        bp.pinyin_name AS pinyinName
        FROM
        (
        SELECT
        bbp.bill_pay_id AS billPayId,
        bbp.bill_id AS billId,
        bbp.org_id AS payeeOrgId,
        bo.abbreviation AS payeeOrgName,
        DATE_FORMAT(bbp.payee_date, '%Y-%m-%d') AS payeeDate,
        bbp.payee_user_id AS payeeId,
        be.employee_name AS payeeName,
        bbp.received_amount AS receivedAmount,
        bb.org_id AS billOrgId,
        bb.bill_num AS billNum,
        bb.order_amount AS originalAmount,
        DATE_FORMAT(bb.bill_date, '%Y-%m-%d') AS billDate,
        bb.privilege_amount AS privilegeAmount,
        bb.actual_amount AS actualAmount,
        bb.received_amount AS totalReceivedAmount,
        btp.registered_dentist_id AS regDentistId,
        btp.patient_id AS patientId
        FROM
        base_bill_pay bbp
        INNER JOIN base_organization bo ON bbp.org_id = bo.org_id
        INNER JOIN (
        SELECT
        bb.org_id,
        bb.bill_id,
        bb.bill_num,
        bb.order_amount,
        bb.bill_date,
        bb.privilege_amount,
        bb.actual_amount,
        bb.received_amount
        FROM
        base_bill bb
        <where>
            AND bb.bill_date IS NOT NULL
            <if test="query.orgId != null">
                AND bb.org_id = #{query.orgId}
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bb ON bbp.bill_id = bb.bill_id
        LEFT JOIN base_treatment_process btp ON bbp.treatment_id = btp.treatment_id
        INNER JOIN base_employee be ON bbp.payee_user_id = be.user_id
        <where>
            <if test="query.orgId != null">
                AND bbp.org_id != #{query.orgId}
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bbp
        LEFT JOIN base_patient bp ON bbp.patientId = bp.patient_id
        LEFT JOIN base_organization bo ON bbp.billOrgId = bo.org_id
        LEFT JOIN base_employee be ON bbp.regDentistId = be.user_id
        ) bbp
        <where>
            <if test="query.patientKeyword != null and query.patientKeyword != ''">
                AND concat(IFNULL(bbp.patientName, ''),IFNULL(bbp.pinyinName, ''), IFNULL(bbp.patientMobile, '')) like
                concat('%', #{query.patientKeyword}, '%')
            </if>
            <if test="query.billNum != null and query.billNum != ''">
                AND bbp.billNum LIKE concat('%', #{query.billNum}, '%')
            </if>
            <if test="query.chargeStartDate != null and query.chargeStartDate != ''">
                AND bbp.payeeDate &gt;= #{query.chargeStartDate}
            </if>
            <if test="query.chargeEndDate != null and query.chargeEndDate != ''">
                AND bbp.payeeDate &lt;= #{query.chargeEndDate}
            </if>
        </where>
        ORDER BY
        bbp.payeeDate DESC,
        bbp.billNum DESC
    </select>

    <!-- 根据条件查询门诊被代收账单（非当月）明细列表 -->
    <select id="selectOtherBillIsAcceptedDetailList"
            resultType="com.yunya.feign.report.domain.vo.StatementBillChargeDetailVO">
        SELECT
        billPayId,
        payeeDate,
        payeeOrgId,
        payeeOrgName,
        billId,
        billNum,
        billOrgId,
        billOrgName,
        billDate,
        patientId,
        patientName,
        patientMobile,
        regDentistId,
        regDentistName,
        originalAmount,
        privilegeAmount,
        actualAmount,
        totalReceivedAmount,
        receivedAmount,
        payeeId,
        payeeName
        FROM (
        SELECT
        bbp.billPayId,
        bbp.payeeDate,
        bbp.payeeOrgId,
        bbp.payeeOrgName,
        bbp.payeeId,
        bbp.payeeName,
        bbp.receivedAmount,
        bbp.billId,
        bbp.billOrgId,
        bo.abbreviation AS billOrgName,
        bbp.billNum,
        bbp.originalAmount,
        bbp.billDate,
        bbp.privilegeAmount,
        bbp.actualAmount,
        bbp.totalReceivedAmount,
        bbp.regDentistId,
        be.employee_name AS regDentistName,
        bbp.patientId,
        bp.`name` AS patientName,
        bp.mobile AS patientMobile,
        bp.pinyin_name AS pinyinName
        FROM
        (
        SELECT
        bbp.bill_pay_id AS billPayId,
        bbp.bill_id AS billId,
        bbp.org_id AS payeeOrgId,
        bo.abbreviation AS payeeOrgName,
        DATE_FORMAT(bbp.payee_date, '%Y-%m-%d') AS payeeDate,
        bbp.payee_user_id AS payeeId,
        be.employee_name AS payeeName,
        bbp.received_amount AS receivedAmount,
        bb.org_id AS billOrgId,
        bb.bill_num AS billNum,
        bb.order_amount AS originalAmount,
        DATE_FORMAT(bb.bill_date, '%Y-%m-%d') AS billDate,
        bb.privilege_amount AS privilegeAmount,
        bb.actual_amount AS actualAmount,
        bb.received_amount AS totalReceivedAmount,
        btp.registered_dentist_id AS regDentistId,
        btp.patient_id AS patientId
        FROM
        base_bill_pay bbp
        INNER JOIN base_organization bo ON bbp.org_id = bo.org_id
        INNER JOIN (
        SELECT
        bb.org_id,
        bb.bill_id,
        bb.bill_num,
        bb.order_amount,
        bb.bill_date,
        bb.privilege_amount,
        bb.actual_amount,
        bb.received_amount
        FROM
        base_bill bb
        <where>
            AND bb.bill_date IS NOT NULL
            <if test="query.orgId != null">
                AND bb.org_id = #{query.orgId}
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m-%d' ) &lt; #{query.startDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y-%m' ) &lt; #{query.startDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bb.bill_date, '%Y' ) &lt; #{query.startDate}
                </if>
            </if>
        </where>
        ) bb ON bbp.bill_id = bb.bill_id
        LEFT JOIN base_treatment_process btp ON bbp.treatment_id = btp.treatment_id
        INNER JOIN base_employee be ON bbp.payee_user_id = be.user_id
        <where>
            <if test="query.orgId != null">
                AND bbp.org_id != #{query.orgId}
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bbp
        LEFT JOIN base_patient bp ON bbp.patientId = bp.patient_id
        LEFT JOIN base_organization bo ON bbp.billOrgId = bo.org_id
        LEFT JOIN base_employee be ON bbp.regDentistId = be.user_id
        ) bbp
        <where>
            <if test="query.patientKeyword != null and query.patientKeyword != ''">
                AND concat(IFNULL(bbp.patientName, ''),IFNULL(bbp.pinyinName, ''), IFNULL(bbp.patientMobile, '')) like
                concat('%', #{query.patientKeyword}, '%')
            </if>
            <if test="query.billNum != null and query.billNum != ''">
                AND bbp.billNum LIKE concat('%', #{query.billNum}, '%')
            </if>
            <if test="query.chargeStartDate != null and query.chargeStartDate != ''">
                AND bbp.payeeDate &gt;= #{query.chargeStartDate}
            </if>
            <if test="query.chargeEndDate != null and query.chargeEndDate != ''">
                AND bbp.payeeDate &lt;= #{query.chargeEndDate}
            </if>
        </where>
        ORDER BY
        bbp.payeeDate DESC,
        bbp.billNum DESC
    </select>
    <!-- 根据条件查询门诊某段时间内收费总额（本门诊账单） -->
    <select id="selectTotalReceivedAmount" resultType="java.math.BigDecimal">
        SELECT
        IFNULL(SUM( bbp.received_amount ), 0) AS totalReceivedAmount
        FROM
        (
        SELECT
        bbp.received_amount,
        bb.org_id AS billOrgId
        FROM
        base_bill_pay bbp
        INNER JOIN base_bill bb ON bbp.bill_id = bb.bill_id
        <where>
            AND bbp.received_amount > 0
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bbp
        <where>
            <if test="query.orgIds != null and query.orgIds.length > 0">
                AND bbp.billOrgId IN
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
        </where>
    </select>
    <!-- 根据条件查询门诊首次收费总额（本门诊账单） -->
    <select id="selectClinicFirstReceivedAmount" resultType="java.math.BigDecimal">
        SELECT
        IFNULL(SUM( bbp.received_amount ), 0) AS totalClinicReceivedAmount
        FROM
        (
        SELECT
        bbp.received_amount,
        bb.org_id AS billOrgId
        FROM
        base_bill_pay bbp
        INNER JOIN base_bill bb ON bbp.bill_id = bb.bill_id and bbp.payee_date = bb.bill_date
        <where>
            AND bbp.received_amount > 0
            <if test="query.orgIds != null and query.orgIds.length > 0">
                AND bbp.org_id IN
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bbp
        <where>
            <if test="query.orgIds != null and query.orgIds.length > 0">
                AND bbp.billOrgId IN
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
        </where>
    </select>
    <!-- 根据条件查询本门诊收欠费收费总额（本门诊账单） -->
    <select id="selectClinicArrearsReceivedAmount" resultType="java.math.BigDecimal">
        SELECT
        IFNULL(SUM( bbp.received_amount ), 0) AS totalClinicReceivedAmount
        FROM
        (
        SELECT
        bbp.received_amount,
        bb.org_id AS billOrgId
        FROM
        base_bill_pay bbp
        INNER JOIN base_bill bb ON bbp.bill_id = bb.bill_id and bbp.payee_date != bb.bill_date
        <where>
            AND bbp.received_amount > 0
            <if test="query.orgIds != null and query.orgIds.length > 0">
                AND bbp.org_id IN
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bbp
        <where>
            <if test="query.orgIds != null and query.orgIds.length > 0">
                AND bbp.billOrgId IN
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
        </where>
    </select>
    <!-- 根据条件查询门诊被代收收费总额（本门诊账单） -->
    <select id="selectClinicBeCollectedReceivedAmount" resultType="java.math.BigDecimal">
        SELECT
        IFNULL(SUM( bbp.received_amount ), 0) AS totalClinicReceivedAmount
        FROM
        (
        SELECT
        bbp.received_amount,
        bb.org_id AS billOrgId
        FROM
        base_bill_pay bbp
        INNER JOIN base_bill bb ON bbp.bill_id = bb.bill_id
        <where>
            AND bbp.received_amount > 0
            <if test="query.orgIds != null and query.orgIds.length > 0">
                AND bbp.org_id NOT IN
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bbp
        <where>
            <if test="query.orgIds != null and query.orgIds.length > 0">
                AND bbp.billOrgId IN
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
        </where>
    </select>
    <!-- 根据条件查询收费记录ID、订单ID列表 -->
    <select id="selectBillIdsAndBillPayIds" resultType="com.yunya.feign.report.domain.vo.BillIdAndBillPayIdVO">
        SELECT
        bbp.billPayId,
        bbp.receivedAmount,
        bbp.payeeOrgId,
        bbp.payeeDate,
        bbp.billOrgId,
        bbp.billId,
        bbp.billDate,
        bbp.actualAmount
        FROM
        (
        SELECT
        bbp.bill_pay_id AS billPayId,
        bbp.received_amount AS receivedAmount,
        bbp.org_id AS payeeOrgId,
        bbp.payee_date AS payeeDate,
        bb.org_id AS billOrgId,
        bb.bill_id AS billId,
        bb.bill_date AS billDate,
        bb.actual_amount AS actualAmount
        FROM
        base_bill_pay bbp
        INNER JOIN (
        SELECT
        bb.org_id,
        bb.bill_id,
        bb.bill_date,
        bb.actual_amount
        FROM
        base_bill bb
        <where>
            AND bb.bill_date IS NOT NULL
            <if test="query.orgIds != null and query.orgIds.length > 0">
                AND bb.org_id IN
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
        </where>
        ) bb ON bbp.bill_id = bb.bill_id
        <where>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        ) bbp
    </select>
    <!-- 根据条件查询账单首次收费收费记录ID列表 -->
    <select id="selectBillFirstPayIdList" resultType="com.yunya.feign.report.domain.vo.BillIdAndBillPayIdVO">
        SELECT
        bbp.bill_pay_id AS billPayId,
        bbp.bill_id AS billId
        FROM
        base_bill_pay bbp,
        base_bill bb
        <where>
            AND bbp.org_id = bb.org_id
            AND bbp.bill_id = bb.bill_id
            AND bbp.payee_date = bb.bill_date
            AND bbp.org_id = bb.privilege_org_id
            AND bbp.payee_date = bb.privilege_date
            <if test="query.orgIds != null and query.orgIds.length > 0">
                AND bbp.org_id IN
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
    </select>
    <!-- 根据条件查询账单收欠费收费收费记录ID列表 -->
    <select id="selectBillArrearsPayIdList" resultType="com.yunya.feign.report.domain.vo.BillIdAndBillPayIdVO">
        SELECT
        bbp.bill_pay_id AS billPayId,
        bbp.bill_id AS billId
        FROM
        base_bill_pay bbp,
        base_bill bb
        <where>
            AND bbp.org_id = bb.org_id
            AND bbp.bill_id = bb.bill_id
            AND bbp.payee_date != bb.bill_date
            AND bbp.org_id = bb.privilege_org_id
            AND bbp.payee_date != bb.privilege_date
            <if test="query.orgIds != null and query.orgIds.length > 0">
                AND bbp.org_id IN
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
    </select>
    <!-- 根据条件查询账单被代收收费收费记录ID列表 -->
    <select id="selectBillBeCollectedPayIdList"
            resultType="com.yunya.feign.report.domain.vo.BillIdAndBillPayIdVO">
        SELECT
        bbp.bill_pay_id AS billPayId,
        bbp.bill_id AS billId
        FROM
        base_bill_pay bbp,
        base_bill bb
        <where>
            AND bbp.org_id = bb.org_id
            AND bbp.bill_id = bb.bill_id
            AND bbp.payee_date != bb.bill_date
            AND bbp.org_id != bb.privilege_org_id
            AND bbp.payee_date != bb.privilege_date
            <if test="query.orgIds != null and query.orgIds.length > 0">
                AND bbp.org_id NOT IN
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
    </select>

    <!-- 查询订单支付记录 -->
    <select id="findBaseBillPayInfoList" resultType="com.yunya.models.report.BaseBillPay">
        select DISTINCT bbp.bill_id billId,bbp.bill_pay_id billPayId,bbp.received_amount receivedAmount  from base_bill_pay bbp
        join base_bill_pay_detail bbpd on bbpd.bill_pay_id = bbp.bill_pay_id
        where 1 = 1
        <if test="itemIds != null">
            and bbpd.account_item_id in
            <foreach collection="itemIds" item="itemId" open="(" separator="," close=")">
                #{itemId}
            </foreach>
        </if>

        <if test="startDate != null">
            and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= DATE_FORMAT( #{startDate}, '%Y-%m-%d' )
        </if>

        <if test="endDate != null">
            and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= DATE_FORMAT( #{endDate}, '%Y-%m-%d' )
        </if>

        <if test="billIdList != null and billIdList.size() > 0 ">
            and bbpd.bill_id in
            <foreach collection="billIdList" item="BillIdVo" open="(" separator="," close=")">
                #{BillIdVo.billId}
            </foreach>
        </if>
    </select>

    <!-- 查询免单订单支付记录 -->
    <select id="findMdBaseBillPayInfoList" resultType="com.yunya.models.report.BaseBillPay">
        select DISTINCT
            bbpd.bill_pay_detail_record_id billPayDetailRecordId,
            bbp.bill_id billId,
            bbp.bill_pay_id billPayId,
            bbpd.principal_amount + bbpd.bonus_amount receivedAmount
        from base_bill_pay bbp
        join base_bill_pay_detail bbpd on bbpd.bill_pay_id = bbp.bill_pay_id
        where 1 = 1
        <if test="itemIds != null">
            and bbpd.account_item_id in
            <foreach collection="itemIds" item="itemId" open="(" separator="," close=")">
                #{itemId}
            </foreach>
        </if>

        <if test="startDate != null">
            and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= DATE_FORMAT( #{startDate}, '%Y-%m-%d' )
        </if>

        <if test="endDate != null">
            and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= DATE_FORMAT( #{endDate}, '%Y-%m-%d' )
        </if>

        <if test="billIdList != null and billIdList.size() > 0 ">
            and bbpd.bill_id in
            <foreach collection="billIdList" item="BillIdVo" open="(" separator="," close=")">
                #{BillIdVo.billId}
            </foreach>
        </if>
    </select>

    <!-- 根据订单id查询支付记录 -->
    <select id="selectBaseBillPayInfoList" resultType="com.yunya.models.report.BaseBillPay">
        select DISTINCT bbp.bill_id billId,bbp.bill_pay_id billPayId,bbp.received_amount receivedAmount from base_bill_pay bbp
        join base_bill_pay_detail bbpd on bbpd.bill_pay_id = bbp.bill_pay_id
        where 1 = 1
        <if test=" billIdList != null and billIdList.size() > 0">
            and bbp.bill_id in
            <foreach collection="billIdList" item="billId" open="(" separator="," close=")">
                #{billId}
            </foreach>
        </if>

        <if test="query.startDate != null">
            and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= DATE_FORMAT( #{query.startDate}, '%Y-%m-%d' )
        </if>

        <if test="query.endDate != null">
            and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= DATE_FORMAT( #{query.endDate}, '%Y-%m-%d' )
        </if>

        <if test="itemIds != null and itemIds.size() > 0">
            and bbpd.account_item_id in
            <foreach collection="itemIds" item="itemId" open="(" separator="," close=")">
                #{itemId}
            </foreach>
        </if>
    </select>

    <!-- 根据订单id查询免单支付记录 -->
    <select id="selectMdBaseBillPayInfoList" resultType="com.yunya.models.report.BaseBillPay">
        select DISTINCT bbpd.bill_pay_detail_record_id billPayDetailRecordId,bbp.bill_id billId,bbp.bill_pay_id billPayId,bbpd.principal_amount + bbpd.bonus_amount receivedAmount from base_bill_pay bbp
        join base_bill_pay_detail bbpd on bbpd.bill_pay_id = bbp.bill_pay_id
        where 1 = 1
        <if test=" billIdList != null and billIdList.size() > 0">
            and bbp.bill_id in
            <foreach collection="billIdList" item="billId" open="(" separator="," close=")">
                #{billId}
            </foreach>
        </if>

        <if test="query.startDate != null">
            and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= DATE_FORMAT( #{query.startDate}, '%Y-%m-%d' )
        </if>

        <if test="query.endDate != null">
            and DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= DATE_FORMAT( #{query.endDate}, '%Y-%m-%d' )
        </if>

        <if test="itemIds != null and itemIds.size() > 0">
            and bbpd.account_item_id in
            <foreach collection="itemIds" item="itemId" open="(" separator="," close=")">
                #{itemId}
            </foreach>
        </if>
    </select>
    <!-- 根据条件查询门诊当月免单收费金额 -->
    <select id="selectCurrentMonthTotalFreePayAmount" resultType="java.math.BigDecimal">
        select sum(principal_amount)
        from base_bill_pay_detail bbpd
        inner join base_bill_pay bbp on bbpd.bill_pay_id = bbp.bill_pay_id
        inner join base_bill bb on bbp.bill_id = bb.bill_id
        <where>
            bbpd.account_item_id in (23, 26)
            <if test="query.queryDate != null and query.queryDate != ''">
                and date_format(bbp.payee_date, '%Y-%m') = #{query.queryDate}
                and date_format(bb.bill_date, '%Y-%m') = #{query.queryDate}
            </if>
            <if test="query.orgId != null">
                and bb.org_id = #{query.orgId}
            </if>
        </where>
    </select>

    <select id="selectRecievedWorkloadsGroupByMonth" resultType="com.yunya.feign.report.domain.vo.BillWorkloadVO">
        SELECT
        DATE_FORMAT(bbp.payee_date,'%Y-%m') AS month,
        bb.bill_id AS billId,
        bb.org_id AS billOrgId,
        IFNULL(bb.actual_amount,0) AS actualAmount,
        IFNULL(SUM(bbp.received_amount),0) AS receivedAmount,
        IFNULL(SUM(bbpd.freePayAmount),0) AS freePayAmount
        FROM
        base_bill_pay bbp
        LEFT JOIN (
            SELECT
            bb.org_id,
            bb.bill_id,
            bb.bill_date,
            bb.actual_amount
            FROM
            base_bill bb
            <where>
                AND bb.bill_date IS NOT NULL
                AND bb.actual_amount &gt; 0
                <if test="query.orgIds != null and query.orgIds.length > 0">
                    AND bb.org_id IN
                    <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                        #{item}
                    </foreach>
                </if>
            </where>
        ) bb ON bbp.bill_id = bb.bill_id
        LEFT JOIN (
            SELECT bill_pay_id,IFNULL(SUM(principal_amount),0) AS freePayAmount FROM base_bill_pay_detail
            WHERE account_item_id IN (23,26) AND type=2
            GROUP BY bill_pay_id
        ) bbpd ON bbpd.bill_pay_id=bbp.bill_pay_id
        <where>
            AND bbp.received_amount &gt; 0
            <if test="query.orgIds != null and query.orgIds.length > 0">
                AND bb.org_id IN
                <foreach collection="query.orgIds" index="index" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="query.dateType == 0">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 1">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y-%m' ) &lt;= #{query.endDate}
                </if>
            </if>
            <if test="query.dateType == 2">
                <if test="query.startDate != null and query.startDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &gt;= #{query.startDate}
                </if>
                <if test="query.endDate != null and query.endDate != ''">
                    AND DATE_FORMAT( bbp.payee_date, '%Y' ) &lt;= #{query.endDate}
                </if>
            </if>
        </where>
        group by
        bb.bill_id,
        bb.org_id,
        month
    </select>

    <!-- 根据条件查询活动推荐患者人数 -->
    <select id="selectActivityRecommendationNumber"
            resultType="com.yunya.feign.patient_central.domain.vo.web.MarketRecommendationVo">
        select
        bpo.id activityIds,
        bpo.name activityName
        from base_patient_origin bpo
        where
        bpo.origin_type = 3
        and bpo.parent_id != 0
        <if test="query.activityIds != null and query.activityIds.size() > 0">
            and bpo.id in
            <foreach collection="query.activityIds" item="activityId" open="(" separator="," close=")">
                #{activityId}
            </foreach>
        </if>

    </select>

    <!-- 根据条件查询实收合计 -->
    <select id="selectTotalAmountPaid" resultType="java.math.BigDecimal">
        SELECT
        IFNULL( sum( t.receivedAmount ), 0 )
        FROM (

        SELECT
        IFNULL( bbp.received_amount , 0 )  receivedAmount
        from base_bill_pay bbp
        join (
            select
            bb.bill_id
            from
            base_bill_pay bbp
            join base_bill bb 	 on bb.bill_id = bbp.bill_id
            join base_patient bp on bp.patient_id = bb.patient_id
            where
            bp.origin_id = #{form.activityIds}
            <if test="query.startDate != null and query.startDate != ''">
                AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
            </if>
            <if test="query.endDate != null and query.endDate != ''">
                AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
            </if>
            GROUP BY bb.bill_id
            ) t   on t.bill_id = bbp.bill_id
            join base_bill_pay_detail bbpd on bbpd.bill_pay_id = bbp.bill_pay_id
        where 1 = 1

            <if test="query.startDate != null and query.startDate != ''">
                AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
            </if>

            <if test="query.endDate != null and query.endDate != ''">
                AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
            </if>

            <if test="itemIds != null and itemIds.size() > 0">
                and bbpd.account_item_id in
                <foreach collection="itemIds" item="itemId" open="(" separator="," close=")">
                    #{itemId}
                </foreach>
            </if>

        GROUP BY bbp.bill_pay_id
        ) t

    </select>

    <!-- 根据条件查询退费合计 -->
    <select id="selectTotalRefundAmount" resultType="java.math.BigDecimal">
        select IFNULL(sum(br.refund_amount),0)
        from base_refund br
        join base_patient bp on bp.patient_id = br.patient_id
        where
        bp.origin_id = #{form.activityIds}
        <if test="query.startDate != null and query.startDate != ''">
            AND DATE_FORMAT( br.refund_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
        </if>
        <if test="query.endDate != null and query.endDate != ''">
            AND DATE_FORMAT( br.refund_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
        </if>
    </select>

    <!-- 市场推荐列表详情 -->
    <select id="selectMarketRecommendationDetailed"
            resultType="com.yunya.feign.patient_central.domain.vo.web.MarketRecommendationDetailedVo">

        select
        bp.name patientName,
        bp.medical_number medicalRecordNumber,
        bbp.bill_pay_id as billPayId ,
        date_format(bbp.payee_date, '%Y-%m-%d') as payDate,
        date_format(bb.order_date, '%Y-%m-%d') as orderDate,
        bbp.received_amount actualAmount,
        sum(
        CASE
        WHEN bbpd.account_item_id = 23 THEN
        bbpd.principal_amount
        WHEN bbpd.account_item_id = 26 THEN
        bbpd.principal_amount ELSE 0
        END
        )  AS freePayment,
        null as refundAmount

        from
        base_bill_pay bbp
        join base_bill bb 	 on bb.bill_id = bbp.bill_id
        join base_patient bp on bp.patient_id = bb.patient_id
        join base_bill_pay_detail bbpd on bbpd.bill_pay_id = bbp.bill_pay_id

        where
        bp.origin_id = #{query.activityIds}
        <if test="query.startDate != null and query.startDate != ''">
            AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
        </if>

        <if test="query.endDate != null and query.endDate != ''">
            AND DATE_FORMAT( bbp.payee_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
        </if>

        union

        select
        bp.name patientName,
        bp.medical_number medicalRecordNumber,
        null as billPayId,
        null as payDate,
        date_format(bb.order_date, '%Y-%m-%d') as orderDate,
        null as actualAmount,
        null as freePayment,
        br.refund_amount refundAmount
        from base_refund br
        join base_patient bp on bp.patient_id = br.patient_id
        join base_bill bb 	 on bb.bill_id = br.bill_id
        where
        bp.origin_id = #{query.activityIds}
        <if test="query.startDate != null and query.startDate != ''">
            AND DATE_FORMAT( br.refund_date, '%Y-%m-%d' ) &gt;= #{query.startDate}
        </if>
        <if test="query.endDate != null and query.endDate != ''">
            AND DATE_FORMAT( br.refund_date, '%Y-%m-%d' ) &lt;= #{query.endDate}
        </if>
    </select>
</mapper>